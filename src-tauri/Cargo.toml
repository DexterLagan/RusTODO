[package]
name = "rustodo"
version = "0.1.0"
description = "A tiny todo list that lives on your desktop."
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
rfd = "0.15"
